package memnets.awt.demo;

import memnets.models.StandardLibrary;

public class JLorentz {
    public static void main(String[] args) {
        JDemo demo = new JDemo(StandardLibrary.lorentz());
        demo.launch(args);
    }
}
